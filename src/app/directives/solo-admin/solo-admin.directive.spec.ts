import { SoloAdminDirective } from './solo-admin.directive';

describe('SoloAdminDirective', () => {
  it('should create an instance', () => {
    const directive = new SoloAdminDirective();
    expect(directive).toBeTruthy();
  });
});
