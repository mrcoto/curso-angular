import { RutMaskDirective } from './rut-mask.directive';

describe('RutMaskDirective', () => {
  it('should create an instance', () => {
    const directive = new RutMaskDirective();
    expect(directive).toBeTruthy();
  });
});
